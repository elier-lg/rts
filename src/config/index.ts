interface configProps {
    feedbackUrl: string;
    helpUrl: string;
}

const config = {
    feedbackUrl: "https://fsl.com/feedback",
    helpUrl: "https://fsl/help"
} as configProps;

export default config;